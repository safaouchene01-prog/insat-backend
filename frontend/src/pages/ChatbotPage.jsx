// frontend/src/pages/ChatbotPage.jsx
/*import Chatbot from "../components/Chatbot/Chatbot";

export default function ChatbotPage() {
  // Replace with however you store the logged-in patient
  // e.g. from context, zustand, localStorage, etc.
  const patientId = JSON.parse(localStorage.getItem("patient"))?.id;

  return (
    <div style={{ height: "100vh", padding: "24px", background: "#eef0f8" }}>
      <Chatbot patientId={patientId} agentId={1} apiUrl={API_URL} />
    </div>
  );
}*/